# Como contribuir

Abra uma issue antes de mudanças grandes. Mantenha alterações pequenas, adicione testes quando houver mudança de comportamento e execute `npm run check` e `npm test` antes de abrir um pull request. Nunca inclua tokens, `config.json`, dados reais de rede ou certificados.
